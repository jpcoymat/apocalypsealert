<%= form_for(@buyer_group) do |f| %>
  <% if @buyer_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@buyer_group.errors.count, "error") %> prohibited this buyer_group from being saved:</h2>

      <ul>
      <% @buyer_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :organization_id %><br>
    <%= f.number_field :organization_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
